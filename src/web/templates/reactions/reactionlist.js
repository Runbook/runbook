<script type="text/javascript">
var options = {
    valueNames: [ 'service', 'name', 'category', 'description' ],
    page: 10,
    plugins: [
      ListPagination({})
    ]
};

var reactionList = new List('reaction-list', options);
</script>
